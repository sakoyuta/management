{
  "name": "sales-training-dashboard",
  "version": "1.0.0",
  "description": "営業育成管理ダッシュボード",
  "main": "src/index.js",
  "homepage": "https://sakoyuta.github.io/management",
  "scripts": {
    "start": "parcel public/index.html",
    "build": "parcel build public/index.html --dist-dir dist --public-url ./",
    "dev": "parcel public/index.html --open",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "parcel": "^2.10.0",
    "@parcel/transformer-react-refresh-wrap": "^2.10.0",
    "process": "^0.11.10",
    "gh-pages": "^6.0.0"
  },
  "browserslist": [
    "> 0.5%",
    "last 2 versions",
    "not dead"
  ]
}